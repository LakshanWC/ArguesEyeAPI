name: Keep Backend Alive

on:
  schedule:
#     # Run every 40 mins
    - cron: "*/10 * * * *"
  workflow_dispatch: 

jobs:
  ping:
    runs-on: ubuntu-latest

    steps:
      - name: Call hello endpoint
        run: |
          curl -f --max-time 20 -v https://argueseyeapi.onrender.com/management/hello
